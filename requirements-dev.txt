# Dev / classical tooling lock aligned to pyproject.toml extras.
# Source of truth: pyproject.toml [project.optional-dependencies] `dev` (+ core).
# Prefer `pip install -e ".[dev]"` so extras stay in sync with packaging metadata.
# Pins below are range-aligned (not inventing tighter pins than pyproject).
-r requirements.txt
pytest>=7.4
pytest-cov>=4.1
ruff>=0.4
mypy>=1.8
build>=1.0
pandas-stubs>=2.0
httpx>=0.27
sphinx
sphinx-rtd-theme
myst-parser
# Engines commonly used in local classical work (also in pyproject engines extra).
polars>=0.20
duckdb>=0.10
