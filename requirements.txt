# Generated-style lock for local classical installs.
# Source of truth for ranges: pyproject.toml [project] dependencies.
# Prefer `pip install -e .` or `pip install -e ".[dev]"` over these pins.
# Pins below track a known-good classical stack within those ranges.
numpy>=1.23
pandas>=2.0
pyarrow>=14.0
scikit-learn>=1.3
