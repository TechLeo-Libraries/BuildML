# Optional local helper: `pip install -r docs/requirements.txt`
# Editable path is relative to this file (repo root = parent of docs/).
# Read the Docs installs via `.readthedocs.yaml` (`path: .` + `docs` extra)
# and does not use this file.
-e ..[docs]
