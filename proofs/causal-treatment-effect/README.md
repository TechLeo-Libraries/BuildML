# causal-treatment-effect

## Business purpose
Estimate average treatment effect under declared backdoor assumptions (AIPW).

## Data source
Synthetic / in-script license-clear data generated by `script.py`.

## Leakage controls
- Proper split before fit (random / group / time as domain requires)
- Train-only fitting and selection
- Holdout evaluation after model lock
- Test never used for selection

## BuildML API steps
1. ingest → split → declare_causal_assumptions
2. fit_causal → evaluate_causal → refute_causal
3. bundle

## Metrics
See `results/results.json` after a successful run.

## Industry comparison (Tier C)

Filled — `baseline_industry.py` estimates AIPW with sklearn nuisance models + placebo refute on the same split (`results/comparison.json`).
## Limitations
Assumptions are caller-declared; EDA is non-causal.
